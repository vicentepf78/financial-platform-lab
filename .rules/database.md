# Database Guidelines

## Migrations

- Flyway with explicit migrations
- Never use `ddl-auto=create` or `ddl-auto=create-drop` in production

## PostgreSQL

- UUID identifiers
- Proper indexes
- Explicit foreign keys
